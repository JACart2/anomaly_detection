# AAD Configuration File
# Controls artifact generation and API trigger behavior.

#The amount of time between api calls
api_frequency_seconds: 15

#The max amount of messages stored
cache_max_items: 100
cache_max_age_seconds: 30

# The LLM info used to select the model in llm_client.py
# Args:
#   model_provider: Name of model provider 
#   model: Name of specific model
llm: 
  model_provider: "openai"
  model: 'gpt-4o'

## TODO replace logic with ROSy way in sprint 3
trigger_script: False

raw_input_topic: "/ai_anomaly_logging"
alert_topic: "/aad/alerts"

throttle_info: true
info_min_period_sec: 0.1

log_every_n_msgs: 1

#Additional bagging info
api_artifact_bagging_enabled: true
api_artifact_output_dir: "/tmp/aad_api_bags"
api_artifact_window_seconds: 3.0
api_artifact_startup_delay_seconds: 0.5

api_artifact_trigger_topic: "/aad/api_bag_trigger"
api_artifact_cache_topic: "/aad/cache_snapshot"
api_artifact_response_topic: "/aad/api_response"

raw_history_max_items: 50
