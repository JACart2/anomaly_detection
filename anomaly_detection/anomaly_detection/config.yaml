# AAD Configuration File
# Controls artifact generation and API trigger behavior.

artifact_frequency_seconds: 30
artifact_duration_seconds: 10
api_frequency_seconds: 60

cache_max_items: 100
cache_max_age_seconds: 30

raw_input_topic: "/ai_anomaly_logging"
alert_topic: "/aad/alerts"

throttle_info: true
info_min_period_sec: 1.0

log_every_n_msgs: 50
